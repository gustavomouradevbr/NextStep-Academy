# Modelos Salvos

Este diretório armazenará os arquivos binários dos modelos treinados (.pkl).

**Instrução:**
Para gerar o arquivo `modelo_evasao.pkl` aqui, certifique-se de instalar as dependências e executar o script `model.py` localizado na raiz do projeto antes de rodar o Flask.